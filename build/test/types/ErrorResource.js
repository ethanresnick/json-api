// Generated by LiveScript 1.2.0
(function(){
  var mocha, sinon, chai, ErrorResource, expect, it2;
  mocha = require('mocha');
  sinon = require('sinon');
  chai = require('chai');
  ErrorResource = require('../../lib/types/ErrorResource');
  expect = chai.expect;
  it2 = it;
  describe("Resource type", function(){
    describe("validation", function(){
      it2("type must be errors", function(){
        var er;
        er = new ErrorResource("bobid9", {});
        expect(er.type).to.equal("errors");
        return expect(function(){
          return er.type = 'noterrors';
        }).to['throw'](/type.*errors/);
      });
      it2("should coerce the status to a string", function(){
        var er;
        er = new ErrorResource("bobid9", {
          status: 300
        });
        return expect(deepEq$(er.attrs.status, "300", '===')).to.be['true'];
      });
      return it2("should coerce the code to a string", function(){
        var er;
        er = new ErrorResource("bobid9", {
          code: 1401
        });
        return expect(deepEq$(er.attrs.code, "1401", '===')).to.be['true'];
      });
    });
    return describe("the fromError helper", function(){
      it2("should use the error's statusCode val as status if status not defined", function(){
        var er;
        er = ErrorResource.fromError({
          'statusCode': 300
        });
        expect(deepEq$(er.attrs.status, "300", '===')).to.be['true'];
        er = ErrorResource.fromError({
          'status': 200,
          'statusCode': 300
        });
        return expect(deepEq$(er.attrs.status, "200", '===')).to.be['true'];
      });
      return it2("should default to status 500", function(){
        return expect(ErrorResource.fromError({}).attrs.status).to.equal("500");
      });
    });
  });
  function deepEq$(x, y, type){
    var toString = {}.toString, hasOwnProperty = {}.hasOwnProperty,
        has = function (obj, key) { return hasOwnProperty.call(obj, key); };
    var first = true;
    return eq(x, y, []);
    function eq(a, b, stack) {
      var className, length, size, result, alength, blength, r, key, ref, sizeB;
      if (a == null || b == null) { return a === b; }
      if (a.__placeholder__ || b.__placeholder__) { return true; }
      if (a === b) { return a !== 0 || 1 / a == 1 / b; }
      className = toString.call(a);
      if (toString.call(b) != className) { return false; }
      switch (className) {
        case '[object String]': return a == String(b);
        case '[object Number]':
          return a != +a ? b != +b : (a == 0 ? 1 / a == 1 / b : a == +b);
        case '[object Date]':
        case '[object Boolean]':
          return +a == +b;
        case '[object RegExp]':
          return a.source == b.source &&
                 a.global == b.global &&
                 a.multiline == b.multiline &&
                 a.ignoreCase == b.ignoreCase;
      }
      if (typeof a != 'object' || typeof b != 'object') { return false; }
      length = stack.length;
      while (length--) { if (stack[length] == a) { return true; } }
      stack.push(a);
      size = 0;
      result = true;
      if (className == '[object Array]') {
        alength = a.length;
        blength = b.length;
        if (first) { 
          switch (type) {
          case '===': result = alength === blength; break;
          case '<==': result = alength <= blength; break;
          case '<<=': result = alength < blength; break;
          }
          size = alength;
          first = false;
        } else {
          result = alength === blength;
          size = alength;
        }
        if (result) {
          while (size--) {
            if (!(result = size in a == size in b && eq(a[size], b[size], stack))){ break; }
          }
        }
      } else {
        if ('constructor' in a != 'constructor' in b || a.constructor != b.constructor) {
          return false;
        }
        for (key in a) {
          if (has(a, key)) {
            size++;
            if (!(result = has(b, key) && eq(a[key], b[key], stack))) { break; }
          }
        }
        if (result) {
          sizeB = 0;
          for (key in b) {
            if (has(b, key)) { ++sizeB; }
          }
          if (first) {
            if (type === '<<=') {
              result = size < sizeB;
            } else if (type === '<==') {
              result = size <= sizeB
            } else {
              result = size === sizeB;
            }
          } else {
            first = false;
            result = size === sizeB;
          }
        }
      }
      stack.pop();
      return result;
    }
  }
}).call(this);
